import Pages from "../../components/layout/Pages";

const Spots = ({}) => {

    return (
        <Pages title={"Spots"}>
            <div>
            <h1>My spots</h1>
        </div>
        </Pages>
    );
};

export default Spots;
