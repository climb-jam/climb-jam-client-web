import Pages from "../../components/layout/Pages";

const CragDetails = ({}) => {

    return (
        <Pages title={"Le spot"}>
        <div>

        </div>
        </Pages>
    );
};

export default CragDetails;
