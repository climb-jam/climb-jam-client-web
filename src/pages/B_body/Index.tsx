import Pages from "../../components/layout/Pages";

const Index = ({}) => {

    return (
        <Pages title={"index"}>
            <h1>My index</h1>
        </Pages>
    );
};

export default Index;
