import Pages from "../../components/layout/Pages";

const Settings = ({}) => {

    return (
        <Pages title={"Paramètres"}>
        <div>
            <h1>My settings</h1>
        </div>
        </Pages>
    );
};

export default Settings;
